Default environment configuration for fastrun on clean system
Command to install:
wget -nv -O - "https://github.com/jjay/envconf/tarball/master" | tar -zxvf - && cp -R jjay-envconf-*/.* ~/ && rm -fdr ./jjay-envconf-*
